[package]
name = "thelema_render"
version = "0.1.0"
edition = "2021"
default-run = "thelema_render"

[dependencies]
glam = "0.27.0"
sefirot = { path = "../sefirot/sefirot", features = ["debug", "trace", "glam"] }
sefirot_testbed = { path = "../sefirot/testbed", features = ["video"] }
tiff = "0.9.1"
